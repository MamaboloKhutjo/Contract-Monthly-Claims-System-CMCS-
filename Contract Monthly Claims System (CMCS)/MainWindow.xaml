<Window x:Class="Contract_Monthly_Claims_System__CMCS_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Contract_Monthly_Claims_System__CMCS_"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="750"
        WindowStartupLocation="CenterScreen">
   

    <Window.Resources>
        <Style  TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0,0,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border CornerRadius="5" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0,0,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="Black">
            <Label Content="Contract Monthly Claims System (CMCS)" 
              FontFamily="Arial" 
              FontSize="30" 
              FontWeight="Bold" 
              Foreground="White"
              HorizontalAlignment="Center" 
              VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1">
            <Border CornerRadius="15" BorderBrush="LightGray" BorderThickness="1" Width="400" Height="300" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20" >

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >
                    <TextBlock Text="Username"  Foreground="#737373" FontFamily="Arial" FontSize="23" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,195,6"/>
                    <TextBox  Text=" " Width="300" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="LightGray" BorderThickness="1"  Margin="0,0,0,12"/>

                    <TextBlock Text="Password" Foreground="#737373"  FontSize="23" FontFamily="Arial" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,195,6"/>
                    <TextBox  Text=" " Width="300" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="LightGray" BorderThickness="1" Margin="0,0,0,-50"/>

                    <Button  Content="Login" FontFamily="Arial" FontWeight="Bold" Background="#001a4d"  Foreground="White" Width="300" Height="45" FontSize="16"  Margin="0,60,0,0" Click="Button_Click" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Don't have an account? " 
                         Foreground="#737373" 
                         FontSize="14"/>

                        <Button Content="Sign Up" 
                         Background="Transparent" 
                         Foreground="#0066CC" 
                         BorderThickness="0" 
                         FontSize="14"
                         Cursor="Hand"
                         Click="Button_Click_1"/>
                    </StackPanel>
                </StackPanel>

            </Border>
        </Grid>

    </Grid>
</Window>
